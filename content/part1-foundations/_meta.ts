import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  index: 'סקירה + וידאו',
  tokens: 'Tokens — האטומים של AI',
  'context-window': 'Context Window — ה-RAM של ה-AI',
  quiz: 'בדקו את עצמכם ❓',
}

export default meta
