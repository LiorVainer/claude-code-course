import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  index: 'סקירה + וידאו',
  'agentic-loop': 'הלולאה האג׳נטית — איך Claude Code חושב',
  permissions: 'הרשאות ואבטחה',
  'claude-md': 'CLAUDE.md — קובץ ה-Onboarding של הפרויקט',
  stateless: 'Stateless + /compact — ניהול ה-Context Window',
  models: 'משפחת המודלים + כלל 90/9/1',
  commands: 'Slash Commands — הפקודות השימושיות',
  quiz: 'בדקו את עצמכם ❓',
}

export default meta
